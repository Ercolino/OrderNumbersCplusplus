language: cpp

compiler:
  - g++

install:
  - sudo wget -qO- https://cmake.org/files/v3.5/cmake-3.5.1-Linux-i386.tar.gz | sudo tar xvz
       
before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
